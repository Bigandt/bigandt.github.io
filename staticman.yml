allowedFields: ["name", "message"]

branch: "master"

commitMessage: "Added Staticman comment"

filename: "entry{@timestamp}"

generatedFields:
  date:
    type: date
    options:
      format: "timestamp-seconds"

moderation: false
name: "jeppebigandt.dk"
path: "_data/comments"
requiredFields: ["name", "message"]